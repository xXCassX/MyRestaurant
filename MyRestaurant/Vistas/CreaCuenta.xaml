<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyRestaurant.Vistas.CreaCuenta">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Text="Eres dueño o empleado: "/>
            <Button Grid.Row="1" Text="Crear Cuenta" Style="{StaticResource BtnNormal}" x:Name="btnCuentaEmpleado" Clicked="btnCuentaEmpleado_Clicked"/>
            <Label Grid.Row="2" Text="Eres cliente: "/>
            <Button Grid.Row="3" Text="Crear Cuenta" Style="{StaticResource BtnNormal}" x:Name="btnCliente" Clicked="btnCliente_Clicked"/>
            <Button Grid.Row="4" HorizontalOptions="End" VerticalOptions="End" Style="{StaticResource BtnAcentuado}" Text="Términos y Condiciones"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>